# macOS shell (Swift)

Menu-bar app: hotkeys, Accessibility / clipboard insertion, wizard (later).

See `docs/architecture.md`. Xcode project will be added when UniFFI bindings exist;
this directory is the agreed home for that target.
